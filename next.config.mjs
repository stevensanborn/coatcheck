/** @type {import('next').NextConfig} */
const nextConfig = {

    logging: {
        level: 'debug', // 'error', 'warn', 'info', 'debug'
        fullUrl: true, // Whether to log the full URL
    },
};

export default nextConfig;
